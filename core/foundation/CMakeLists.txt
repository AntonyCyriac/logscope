# ==========================================================
# LogScope
# Core Foundation
# ==========================================================

add_library(scope_foundation INTERFACE)

target_include_directories(scope_foundation
    INTERFACE
        ${CMAKE_CURRENT_SOURCE_DIR}
)
