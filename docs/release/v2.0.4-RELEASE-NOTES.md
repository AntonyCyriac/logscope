# v2.0.4 — Desktop CLI Parity (Phase C)

## Highlights

- Completes **M14.12 Phase C**: configuration editor, open format/profile, session save options, clipboard/stdin open
- Expands **headless GUI tests** to 14 scenarios including `large-app.log` persist-index path
- Adds **`samples/large-app.log`** (~1 MB) for large-log and index workflows

## Added

- **File → Configuration…** — key/value editor, validate, save `.properties`
- **Open…** dialog — log format (auto/plain/jsonl) and profile (`generic-plain`, `generic-json`)
- **Session → Save…** dialog — min errors/warnings/lines, search, investigation bar, report sections
- **File → Open from Clipboard** and **Open Stdin** (`-`)
- `ConfigurationManager::saveToFile()` for properties round-trip
- `logscope_desktop_tests` — profile open, large log + persist index, clipboard, investigate level, session round-trip, dialog smoke tests
- `samples/large-app.log` — ~14k lines for persist/reuse and desktop stress

## Fixed

- Session load re-opens source when saved session has no in-memory table index

## Upgrade notes

- **Desktop-focused** release; CLI unchanged.
- Replace `logscope-desktop` or download new release artifacts.

## Artifacts

Same six archives as `v2.0.3` (CLI + desktop × Linux, Windows, macOS).
