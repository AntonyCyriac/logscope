# v2.0.6 — CI static-analysis hotfix (v2.0.5 follow-up)

## Highlights

- Fixes **clang-tidy CI** failure on `AiPanel::runAsk()` (null pointer static analysis)

## Fixed

- Desktop: null-guard `m_askEdit` / `m_outputEdit` in `AiPanel` before use (`clang-analyzer-core.CallAndMessage`).

## Upgrade notes

- **CI / quality-gate** release; no user-visible desktop behavior change expected.
- Same six versioned artifacts as `v2.0.5` (`logscope-v2.0.6-…`).

## Artifacts

| Platform | CLI | Desktop |
|----------|-----|---------|
| Linux | `logscope-v2.0.6-linux-amd64.tar.gz` | `logscope-desktop-v2.0.6-linux-amd64.tar.gz` |
| Windows | `logscope-v2.0.6-windows-amd64.zip` | `logscope-desktop-v2.0.6-windows-amd64.zip` |
| macOS | `logscope-v2.0.6-macos-amd64.tar.gz` | `logscope-desktop-v2.0.6-macos-amd64.tar.gz` |
