# v1.5.2 — Phase 1 Stabilization

## Highlights

- Phase 1 **Stabilize v1.x** complete — documentation, test hardening, observability, and engineering CI before M14
- CLI `--stats` for parse timing, plugin load metrics, and memory RSS
- Expanded regression guards (AI isolation, plugin failure, storage flake fix)
- CLI matrix agent investigate + query/filter fuzz target
- Third-party license manifest and CI scan

## Added

- `docs/tutorials/` — analyze, investigate, plugins/AI walkthroughs
- `AnalysisStats`, `PluginLoadStats`, and `foundation::currentProcessMemoryUsage()`
- CLI `--stats` on `analyze`, `investigate`, and `agent investigate`
- `tests/regression/ai_regression_test.cpp` and `plugin_regression_test.cpp`
- `query_filter_fuzz` libFuzzer target (filter DSL, search query, regex)
- `third_party/manifest.json` and `scripts/check_third_party_licenses.py`
- CI jobs: license-scan; CLI matrix agent + bad plugin path scenarios
- `samples/plugin-bad-path.properties` for matrix isolation testing

## Changed

- `scripts/run_cli_matrix.py` — agent investigate (noop) and plugin bad-path scenarios
- `PHASE-1-STABILIZATION.md` and scenario tracker — Phase 1 closed at this release
- Component catalog diagram refreshed for M11–M13 modules

## Documentation

- [THIRD_PARTY_LICENSES.md](../handbook/THIRD_PARTY_LICENSES.md)
- [PHASE-1-STABILIZATION.md](../planning/PHASE-1-STABILIZATION.md)
- API docs artifact-only policy in [docs/api/README.md](../api/README.md)

## Upgrade notes

- No breaking changes for default configuration
- `--stats` is opt-in on analyze/investigate/agent commands

## Known limitations

- GitHub Pages for API docs remains optional (CI `api-docs` artifact)
- SIMD / zero-copy parsing deferred to feature milestones
- M14 Desktop Application is the next major milestone

## Test baseline

**520** automated tests (regression expansion, observability, fuzz, license CI)
