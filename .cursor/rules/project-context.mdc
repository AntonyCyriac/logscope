---
description: LogScope assistant bootstrap — AGENTS.md, project context, code map
alwaysApply: true
---

# LogScope project context (public repo)

Canonical bootstrap for any AI assistant: `@AGENTS.md`

When editing **logscope**:

1. `@AGENTS.md` — load order and principles (tool-agnostic)
2. `@docs/handbook/PROJECT_CONTEXT.md` — engineering mindset, current release, milestones
3. `@docs/handbook/CODE_MAP.md` — where CLI/desktop/web code and tests live
4. Current release: `@docs/release/v2.2.1-RELEASE-NOTES.md`

When **logscope-strategy** is in workspace, EngOS routing is optional (private); do not paste strategy content into public PRs.

Shipped-tag regressions: see `release-regression-issues.mdc`.

<!-- discard: CI docs-only waiver smoke test -->
